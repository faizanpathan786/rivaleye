# Direct deployment: pull code to VM and rebuild
steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud compute ssh faizan514pathan@rivaleye-server \
          --zone=us-central1-a \
          --tunnel-through-iap \
          --quiet \
          --command="bash -lc 'cd /home/faizan514pathan/rivaleye-v3 && git reset --hard HEAD && git pull origin main && bash deploy.sh'"

timeout: '7200s'

options:
  logging: CLOUD_LOGGING_ONLY
