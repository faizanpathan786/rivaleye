CONNECTION_STRING=postgresql://postgres:postgres@localhost:5432/rivaleye

BETTER_AUTH_SECRET=change-me-in-prod
BETTER_AUTH_URL=http://localhost:4000
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Scraper credentials (worker)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=rivaleye/0.1
REDDIT_MAX_TERMS=              # cap search terms (9 total: 5 complaint-biased, 4 neutral; use 2-3 for dev)
REDDIT_MAX_POSTS_PER_TERM=     # cap posts per term (default: 50, use 10-15 for dev)
PRODUCTHUNT_TOKEN=
TRUSTPILOT_API_KEY=
X_API_BEARER=
APIFY_TOKEN=
GOOGLE_PLACES_API_KEY=

# LLM (shared client used by api + worker)
OPENROUTER_API_KEY=sk-or-v1-
OPENROUTER_MODEL=deepseek/deepseek-chat
# Sampling temperature for all pipeline LLM calls. Lower = more deterministic
# JSON and fewer parse-retries (faster). Defaults to 0.3.
OPENROUTER_TEMPERATURE=0.3

# Razorpay (billing)
RAZORPAY_KEY_ID=rzp_test_
RAZORPAY_KEY_SECRET=
# Razorpay webhook signing secret (Dashboard → Webhooks). Authoritative credit grant.
RAZORPAY_WEBHOOK_SECRET=

# logo.dev secret key — SERVER-SIDE ONLY, used by the /v1/logo proxy. Never expose as VITE_*.
LOGODEV_SECRET_KEY=

# Frontend
VITE_API_URL=http://localhost:4000
# Base URL of the web app — used by the API's PDF export (headless Chrome loads
# the export view from here). Set to the deployed web origin in production.
WEB_URL=http://localhost:4004
VITE_RAZORPAY_KEY_ID=rzp_test_

# Job orchestration engine: postgres or inngest
# Default: postgres (recommended for MVP)
# Only set INNGEST_EVENT_KEY if using inngest engine
REPORT_PIPELINE_ENGINE=postgres

# Inngest — leave blank for local dev (uses the inngest-cli dev server)
# Only required when REPORT_PIPELINE_ENGINE=inngest
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# Worker ports (Inngest dev server discovers these)
WORKER_SCRAPE_PORT=4001
WORKER_LLM_PORT=4002
WORKER_SYNTH_PORT=4003

# Worker concurrency tuning (optional; defaults shown)
WORKER_MAX_CONCURRENT_SOURCE=8
WORKER_MAX_CONCURRENT_SYNTHESIS=2
WORKER_DB_POOL_MAX=10
